<?php

__('messages.welcome');

__('subfolder/profile.name'); // a valid laravel i18n key with slash
__('subfolder.profile.name'); // it shows inline annotations after replacing shash with dot, but it's an invalid key and it won't work correctly
